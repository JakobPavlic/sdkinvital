.row-fluid
  .span10.offset1
    %div{:id => "fb-root"}
    .post
      = link_to '<i class="icon-chevron-left"></i> Vse objave'.html_safe, posts_path

      %h2.oswald
        =@post.title
      %p
        =link_to @post.user.name, user_path(@post.user)
        %small.muted
          %i{:class => "icon-time"}
          =@post.created_at.strftime("%d %b. %Y")
          ob
          =@post.created_at.strftime("%H:%M")
      %p
        =raw @post.content
      %p
        -if can? :update, @post
          = link_to "<i class='icon-edit'></i> Uredi objavo".html_safe, edit_post_path, :class => "btn"
      -if can? :destroy, @post 
        = button_to "Izbris", post_path(@post), :method => :delete, :confirm => "Ali ste prepričani?",:class => 'btn btn-danger'
      %hr
              
      =render 'layouts/socikone'
           
      =render 'layouts/facebook'
      =render 'layouts/google'
      =render 'layouts/twitter'
      =render 'layouts/disqus'
