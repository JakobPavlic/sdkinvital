%i{:class => "icon-arrow-left"} 
= link_to 'Vse objave', posts_path

%h2
  =@post.title
%small
  %i.icon-user
  = link_to @post.instructor.name, instructor_path(@post.instructor)

%small
  =@post.created_at.strftime("%d %b. %Y")

  =@post.created_at.strftime("%H:%M")

%p
  =@post.content
  
  %div{:id => "fb-root"}
  %div{:class => "fb-like", "data-send" => "true", "data-layout" => "button_count", "data-width" => "450", "data-show-faces" => "true", "data-font" => "arial"}

