.row-fluid
  .span7
    -if can? :create, Post 
      =link_to "<i class = 'icon-pencil'></i> Nova objavo".html_safe, new_post_path, :class => "btn btn-primary"
    - @posts.each do |post|
      .post
        %h3.oswald
          = link_to post.title, post
        %small
          /
            %i{:class => "icon-calendar"}
            =post.created_at.strftime("%d %b. %Y")
            %i{:class => "icon-time"}
            =post.created_at.strftime("%H:%M")
        %p
          = raw post.content
        %hr

