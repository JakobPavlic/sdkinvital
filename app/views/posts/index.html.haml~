.row-fluid
  .span8.offset2
    -if can? :create, Post 
      =link_to "<i class = 'icon-pencil'></i> Nova objava".html_safe, new_post_path, :class => "btn btn-primary"
    - @posts.each do |post|
      .post
        %h3.oswald
          = link_to post.title, post
        %p
          =link_to post.user.name, user_path(post.user)
          %small.muted
            %i{:class => "icon-time"}
            =post.created_at.strftime("%d %b. %Y")
            ob
            =post.created_at.strftime("%H:%M")
        %p
          = raw post.content
        %hr
//=render 'layouts/disqusk'

